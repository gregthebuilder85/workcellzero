/* ============================================================================
   WORKCELL ZERO // LOCAL PREVIEW INPUTS
   ----------------------------------------------------------------------------
   Local-only launch inputs for the private website preview. Every value may
   stay blank. While a value is blank, the matching section renders as a
   clearly labeled slot. No blank value is ever displayed as if it were real,
   and no configuration note appears inside public page copy.

   Fill these only when the corresponding launch input actually exists. Mirror
   of reference PREVIEW_INPUTS.md.
   ============================================================================ */
window.WORKCELL_PREVIEW = {
  founderName: "",            // real accountable maintainer name
  founderImage: "",           // local path to a real founder photo
  founderSocial: "",          // founder social profile URL
  supportEmail: "",           // real support address
  demoUrl: "",                // the real-run screen recording
  publicBuildRecordUrl: "",   // ungated public Build Record
  changelogUrl: "",           // public changelog
  operatorCheckoutUrl: "",    // merchant checkout, stays disabled in preview
  publicDomain: ""            // public domain, when live
};
